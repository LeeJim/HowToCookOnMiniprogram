<!--index.wxml-->
<view class="container">
  <list 
    wx:for="{{list}}" 
    name="{{item.name}}"
    icon="{{item.icon}}"
    wx:key="name"
    bind:click="handleTap"
    list="{{item.list}}" />
</view>

<include src="/templates/tabbar.wxml" />
